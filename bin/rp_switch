#!/usr/bin/env python
# -*- coding: utf-8 -*-

import sys
from oslo_config import cfg
from robophery.gpio.switch import SwitchModule

common_opts = [
    cfg.Opt('name',
            short='n',
            default="switch",
            help='Switch sensor name'),
    cfg.Opt('port',
            short='p',
            help='Switch GPIO port'),
]

CONF = cfg.CONF
CONF.register_cli_opts(common_opts)
CONF(sys.argv[1:])

module = SwitchModule(CONF)
print(module.get_data)
